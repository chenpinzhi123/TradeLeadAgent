000
---

date: 2026-06-16
project: TradeLeadAgent
---

## 今日完成

- 配置 Streamlit Cloud 部署支持：修改 `config.py` 以支持 `st.secrets`（兼容本地 `.env`），创建 `.streamlit/config.toml` 和 `secrets.toml`（含 DeepSeek 配置），更新 `.gitignore` 排除敏感文件。
- 初始化 Git 仓库，将代码推送至 GitHub（https://github.com/chenpinzhi123/TradeLeadAgent.git）。
- 部署计划：先尝试 Streamlit Cloud（免费、最快），备选国内轻量云服务器或香港服务器。
- 用户明确使用 DeepSeek API 作为 LLM 后端。

