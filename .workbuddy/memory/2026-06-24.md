# 2026-06-24 工作日志

## 自我优化（寻客数据质量提升）并部署

### 执行时间
2026-06-24 00:27 ~ 01:23

### 优化内容

#### 1. 修复质量评估评分上限 bug（P0）
- `scripts/quality_evaluator.py`:
  - `scrape_quality` 计算修复：子项是 0-100 百分比，需除以 4 缩放到 0-25 上限（之前虚高到 99.7）
  - `total_score` 增加 `min(100, ...)` 上限保护

#### 2. 优化 scraper.py 抓取能力
- `_fetch_page`:
  - 超时 10s → 15s
  - 增加重试机制（最多 2 次，间隔递增）
  - 放宽 HTML 检测：只检查内容前 2000 字符是否含 `<html`，不再强依赖 content-type
- `scrape_lead`:
  - 初始抓取失败时，自动尝试替代 URL（http/https 互换、www 互换）
  - 新增 `mailto:` 链接邮箱提取
  - 新增 `tel:` 链接电话提取
  - 首页没拿到邮箱时，主动探测常见路径（`/contact`、`/contact-us`、`/about`、`/about-us` 等）
- `_find_priority_pages`:
  - 增加阿拉伯语关键词（اتصل بنا、من نحن 等），提升中东地区联系页面命中率

#### 3. 质量评估结果（修复后真实分数）
- 平均总分：82.4/100（修复前虚高 99.7）
- 搜索质量：20.5/25
- 抓取质量：17.2/25
- 评分质量：22.9/25
- 整体可用性：21.9/25
- 通过率（≥60）：88%（7/8）
- 唯一失败：医疗器械→中东（45.97分，抓取成功率 30%、邮箱质量 0%）

### 部署
- Commit: `d0c4dda`
- Push 到 main 分支成功
- Streamlit Cloud 自动部署中（1-3 分钟）

### 后续建议
- 医疗器械→中东失败根因：中东地区网站可达性差 + 邮箱提取率低
  - 可考虑增加代理 IP 或放宽超时
  - 可考虑用搜索引擎的 `site:example.com "email"` 方式间接获取邮箱
- `email_quality` 普遍偏低（多数 <20%），可考虑从页面 meta 标签、JSON-LD 等结构化数据中抽取邮箱
